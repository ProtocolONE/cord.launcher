// App Shared Variables
// --------------------------------------------------
// To customize the look and feel of this app, you can override
// the Stylus variables found in Quasar's source Stylus files. Setting
// variables before Quasar's Stylus will use these variables rather than
// Quasar's default Stylus variable values. Stylus variables specific
// to the themes belong in either the variables.ios.styl or variables.mat.styl files.

// Check documentation for full list of Quasar variables


// App Shared Color Variables
// --------------------------------------------------
// It's highly recommended to change the default colors
// to match your app's branding.

// --- root
:root
  --vw: 100vw
  --vh: 100vh
  --vh-offset: 0

// --- Theme colors
$primary   = #027be3
$secondary = #26A69A
$tertiary  = #555

// --- Alert colors
$neutral   = #E0E1E2
$positive  = #21BA45
$negative  = #DB2828
$info      = #31CCEC
$warning   = #F2C037

// --- Viewport
$vw = var(--vw)
$vh = calc(var(--vh) * 100 - var(--vh-offset))

// --- Default colors
$white   = #FFF
$black   = #000

// --- Backgrounds colors
$bg-black          = #080808
$body-background   = #E5E5E5
$page-background   = #434343
$header-background = #262626
$footer-background = #262626

// --- Text colors
$text-color-light-1 = #909090
$text-color-light-2 = #818181
$text-color-light-3 = #A9A9A9

// --- media
// --- desktop started width
$desktop-start-w = 1024px

// --- borders
$border-radius = 4px
